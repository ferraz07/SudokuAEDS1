int existenaColuna(int coluna, int numero)
 {

	  for (int i=0; i<9; i++)
	   {
		  if (principal[i][coluna] == numero)
			  return 1;
	   }
}


int existenalinha(int linha, int numero)
{
	   for (int j=0; j<9; j++)
	   {
		   if (principal[linha][j] == numero)
			   return 1;
		}
}
